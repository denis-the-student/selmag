spring:
  datasource:
    url: jdbc:tc:postgresql:16:///selmag #?TC_DAEMON=true
    username: test_user
    password: password
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true