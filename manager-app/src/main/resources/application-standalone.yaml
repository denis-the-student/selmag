selmag:
  service:
    catalogue:
      url: http://localhost:8081
      registration-id: keycloak

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/selmag_manager_db
    username: manager_user
    password: password
  flyway:
    schemas:
      - public
      - user_management
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://localhost:8082/realms/selmag
            user-name-attribute: preferred_username
        registration:
          keycloak:
            client-id: manager-app
            client-secret: TfnyspJhEkW7BbN83DDk67kMTfaJPxXb
            scope:
              - openid
              - view_catalogue
              - edit_catalogue
              - microprofile-jwt


